const { createClient } = require("@supabase/supabase-js"); const fs = require("fs"); const path = require("path"); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function uploadVenuePhotos() { const venuesDir = path.join(process.cwd(), "public", "images", "venues"); const files = fs.readdirSync(venuesDir); for (const file of files) { try { const filePath = path.join(venuesDir, file); const fileBuffer = fs.readFileSync(filePath); console.log(`Uploading ${file}...`); const { data, error } = await supabase.storage.from("venues").upload(file, fileBuffer, { contentType: "image/jpeg", upsert: true }); if (error) { console.error(`Error uploading ${file}:`, error); } else { console.log(`Successfully uploaded ${file}`); } } catch (error) { console.error(`Error processing ${file}:`, error); } } } uploadVenuePhotos();
